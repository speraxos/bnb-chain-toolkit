# Changelog

All notable changes to the ERC-8004 CLI.

## [1.0.0] — 2025-01-15

### Added

- `register` command with interactive agent registration wizard
- `view` command for detailed agent inspection
- `list` command to browse agents by owner
- `search` command with service type filtering
- `update` command for metadata URI updates
- `reputation` command for reputation and feedback viewing
- `init` command to scaffold agent-card.json and ai-plugin.json
- `chains` command with live connectivity checking
- Config file support at `~/.erc8004/config.json`
- Environment variable support for private keys
- Support for BSC Testnet, BSC Mainnet, Ethereum Sepolia, Ethereum Mainnet
- Color-coded terminal output with spinners and tables
